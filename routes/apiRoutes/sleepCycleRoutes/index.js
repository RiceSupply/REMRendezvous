const router = require('express').Router();
const homeRoutes = require('./homeRoutes');
const userRoutes = require('./userRoutes');

// /api/sleepCycleRoutes
router.get('/',);

module.exports = router;